language = "python3"
run = "pip install --user typing-extensions>=4.5.0 && pip install --user openai tweepy anthropic pydantic prompt-toolkit python-dotenv aiohttp requests && python main.py"
entrypoint = "main.py"

[env]
PYTHONPATH = "${PYTHONPATH}:${REPL_HOME}"

[nix]
channel = "stable-22_11"
